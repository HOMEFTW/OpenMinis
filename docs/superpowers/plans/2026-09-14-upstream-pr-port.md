# Android 上游 PR 审查与移植

目标：在当前 fork 上选择性移植，保留已有模型、生图、任务状态、导航和备份预览。

1. #346：启动重注册定时任务；严格识别卸载结果读回，避免重复卸载；WebView 按持有者处理失效、解除等待和恢复。
2. #322：开放本地备份，后台保存/分享并显示错误，保留远程成功后的清理；MCP 手动测试采用安全参数引用、真实退出码及响应校验；用量统计增加日期筛选和模型搜索。
3. #260：利用现有 allowsEmptyAPIKey，只对允许免密的自定义接口开放空密钥保存。
4. 不移植 #322 的无差别工具并发（共享 shell/browser 和写操作不保证独立）；价格数据库/TTS 不在本轮引入，避免未完成备份兼容和额外音频生命周期。#331 的全局 OpenCode 请求头会泄露无关标识，不引入。#309 为 iOS draft，跳过。

验证：针对卸载识别、MCP 响应及参数引用、用量筛选增加 JVM 回归测试；运行 Android JVM 全套和 APK 构建、签名检查。无设备时明确保留 WebView 崩溃/SAF/闹钟真机验收项。不调用付费模型，不直接合并上游整包，不自动发布。

来源：https://github.com/OpenMinis/OpenMinis/pull/346 、 /322 、 /260 、 /331 、 /309 。

实施结果与采纳理由见 [Android 上游 PR 审查与移植](../../android-upstream-pr-review.md)。
