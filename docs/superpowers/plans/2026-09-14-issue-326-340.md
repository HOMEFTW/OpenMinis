# Issue #326/#340 修复计划

- #340：模型目录刷新时对 API 返回条目执行 `ModelsDevApi.enrichModels`，再通过 `replaceEntries` 合并；保留用户明确设置的 modality overrides，同时更新基础模型能力。
- #326：沿用现有 UI tail window、上下文 offload、主动 compact 和请求图片预算，确保 Agent 请求使用摘要与受控历史快照，避免持久审计历史被删除。

验收：Android JVM 测试、APK 构建、签名校验和 `git diff --check`；不调用真实模型 API，不做真机声明。
